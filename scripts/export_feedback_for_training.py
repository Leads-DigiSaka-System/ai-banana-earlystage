# Export feedback (wrong predictions + correct_class) for retraining.
# Output: CSV or folder structure compatible with training pipeline.
# Run: python -m scripts.export_feedback_for_training [--output dir]
# TODO: Implement when database and feedback tables are ready.

if __name__ == "__main__":
    print("TODO: export feedback for training")
